var city = "${sessionScope.city}";
var country = "${sessionScope.country}";
var email = "${sessionScope.email}";
var firstName = "${sessionScope.firstName}";
var lastName = "${sessionScope.lastName}";
var state = "${sessionScope.state}";
var street = "${sessionScope.street}";
var zip = "${sessionScope.zip}";
